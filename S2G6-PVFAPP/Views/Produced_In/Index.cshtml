@model IEnumerable<S2G6_PVFAPP.Models.Produced_In>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Product.productDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Work_Center.workcenterLocation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Product.productDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Work_Center.workcenterLocation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.producedinID }) |
            @Html.ActionLink("Details", "Details", new { id=item.producedinID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.producedinID })
        </td>
    </tr>
}

</table>
