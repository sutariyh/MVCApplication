@model IEnumerable<S2G6_PVFAPP.Models.Has_Skill>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employee.employeeSupervisor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Skill.skillDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.employeeSupervisor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Skill.skillDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.hashskillID }) |
            @Html.ActionLink("Details", "Details", new { id=item.hashskillID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.hashskillID })
        </td>
    </tr>
}

</table>
